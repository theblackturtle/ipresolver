# ipresolver
IP Address Resolver

## Usage
```
Usage of ./ipresolver:
  -i string
        Domains list
  -r string
        Resolver file (Format: ip:port)
  -t int
        Threads to run (default 5)
```