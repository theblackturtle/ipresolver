# ipresolver
IP Address Resolver

## Usage
```
Usage of ./ipresolver:
  -i string
        Domains list
  -only-ip
        Output only IP Addresses
  -r string
        Resolver file (Format: ip:port)
  -t int
        Threads to run (default 5)
```